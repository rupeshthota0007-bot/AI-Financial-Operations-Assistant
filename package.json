{
  "name": "agentic-finops-assistant",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "cd frontend && npm install --include=optional && npm run build",
    "start": "cd frontend && npm run preview",
    "dev": "npm run dev --prefix frontend"
  }
}
